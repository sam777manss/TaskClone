@model RoleEdit
@{
    ViewData["Title"] = "UPDATE ROLE";
}

@*<h1 class="bg-info text-white">Update Role</h1>
<a asp-action="Index" class="btn btn-secondary">Back</a>
<div asp-validation-summary="All" class="text-danger"></div>

<form method="post">
    <input type="hidden" name="roleName" value="@Model.Role.Name" />
    <input type="hidden" name="roleId" value="@Model.Role.Id" />

    <h2 class="bg-info p-1 text-white">Add To @Model.Role.Name</h2>
    <table class="table table-bordered table-sm">
        @if (Model.NonMembers.Count() == 0)
        {
            <tr><td colspan="2">All Users Are Members</td></tr>
        }
        else
        {
            @foreach (AppUser user in Model.NonMembers)
            {
                <tr>
                    <td>@user.UserName</td>
                    <td>
                        <input type="checkbox" name="AddIds" value="@user.Id">
                    </td>
                </tr>
            }
        }
    </table>

    <h2 class="bg-info p-1 text-white">Remove From @Model.Role.Name</h2>
    <table class="table table-bordered table-sm">
        @if (Model.Members.Count() == 0)
        {
            <tr><td colspan="2">No Users Are Members</td></tr>
        }
        else
        {
            @foreach (AppUser user in Model.Members)
            {
                <tr>
                    <td>@user.UserName</td>
                    <td>
                        <input type="checkbox" name="DeleteIds" value="@user.Id">
                    </td>
                </tr>
            }
        }
    </table>
    <button type="submit" class="btn btn-primary">Save</button>
</form>*@


<form method="post">
    <input type="hidden" name="roleName" value="@Model.Role.Name" />
    <input type="hidden" name="roleId" value="@Model.Role.Id" />
<div class="content-wrapper">
    <!-- Content -->
    <div class="container-xxl flex-grow-1 container-p-y">
        <div class="card mb-4">
            <h5 class="card-header">Add To @Model.Role.Name</h5>
            <hr class="m-0">
            <div class="card-body">
                <div class="row">

                    <!-- List group Numbered -->
                    <!--/ List group Numbered -->
                    <!-- List group checkbox -->
                    <div class="col-lg-6">
                        @*<small class="text-light fw-semibold">List Group With Checkbox</small>*@
                        <div class="demo-inline-spacing mt-3">
                                @if (Model.NonMembers.Count() == 0)
                                {
                                    <tr><td colspan="2">All Users Are Members</td></tr>
                                }
                                else
                                {
                                @foreach (AppUser user in Model.NonMembers)
                                {
                                    <div class="list-group">
                                        <label class="list-group-item">
                                                <input class="form-check-input me-1" type="checkbox" name="AddIds" value="@user.Id">
                                            @user.UserName
                                        </label>
                                    </div>
                                }
                                }
                        </div>
                    </div>
                    <!--/ List group checkbox -->
                </div>
            </div>
            <hr class="m-0">
        </div>
    </div>
    <!-- / Content -->
        <!-- Content -->
        <div class="container-xxl flex-grow-1 container-p-y">
            <div class="card mb-4">
                <h5 class="card-header">Remove From @Model.Role.Name</h5>
                <hr class="m-0">
                <div class="card-body">
                    <div class="row">

                        <!-- List group Numbered -->
                        <!--/ List group Numbered -->
                        <!-- List group checkbox -->
                        <div class="col-lg-6">
                            @*<small class="text-light fw-semibold">List Group With Checkbox</small>*@
                            <div class="demo-inline-spacing mt-3">
                                @if (Model.Members.Count() == 0)
                                {
                                    <tr><td colspan="2">No Users Are Members</td></tr>
                                }
                                else
                                {
                                    @foreach (AppUser user in Model.Members)
                                    {
                                        <div class="list-group">
                                            <label class="list-group-item">
                                                <input class="form-check-input me-1" type="checkbox" name="DeleteIds" value="@user.Id">
                                                @user.UserName
                                            </label>
                                        </div>
                                    }
                                }
                            </div>
                        </div>
                        <!--/ List group checkbox -->
                    </div>
                </div>
                <hr class="m-0">
            </div>
        
            <div class="mt-2">
                <button type="submit" class="btn btn-primary me-2">Save changes</button>
                @*<button type="reset" class="btn btn-outline-secondary">Cancel</button>*@
            </div>
        </div>
        <!-- / Content -->
</div>
</form>